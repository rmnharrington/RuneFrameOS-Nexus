apiVersion: apps/v1
kind: Deployment
metadata:
  name: hoardwell
  namespace: runeframeos
  labels:
    app: hoardwell
spec:
  replicas: 1
  selector:
    matchLabels:
      app: hoardwell
  template:
    metadata:
      labels:
        app: hoardwell
    spec:
      tolerations:
      - key: "node-role.kubernetes.io/control-plane"
        operator: "Exists"
        effect: "NoSchedule"
      containers:
      - name: hoardwell
        image: hoardwell:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 3008
        env:
        - name: NODE_ENV
          value: "production"
        - name: PORT
          value: "3008"
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        # Health checks disabled for initial testing
        # livenessProbe: [ADD_LATER]
        # readinessProbe: [ADD_LATER]
---
apiVersion: v1
kind: Service
metadata:
  name: hoardwell
  namespace: runeframeos
  labels:
    app: hoardwell
spec:
  selector:
    app: hoardwell
  ports:
  - port: 3008
    targetPort: 3008
    nodePort: 30004
    protocol: TCP
  type: NodePort
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: hoardwell-ingress
  namespace: runeframeos
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
spec:
  ingressClassName: nginx
  rules:
  - host: hoardwell.pedantictheory.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: hoardwell
            port:
              number: 3008
